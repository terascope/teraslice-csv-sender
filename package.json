{
  "name": "teraslice_csv_sender",
  "version": "1.0.0",
  "description": "Teraslice sender that writes slice content to a file in simple csv format.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.4.0"
  }
}
